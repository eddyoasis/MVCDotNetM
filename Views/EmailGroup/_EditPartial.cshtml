@model EmailGroupEditReq

@using (Html.BeginForm("Edit", "EmailGroup", FormMethod.Post, new { id = "editForm" }))
{
    @Html.AntiForgeryToken()
    @Html.HiddenFor(m => m.ID)

    <div class="modal-header">
        <h5 class="modal-title">Edit Email Group</h5>
        <button type="button" class="close" data-bs-dismiss="modal" aria-label="Close">
            <span>&times;</span>
        </button>
    </div>
    <div class="modal-body">
        <div class="form-group mt-3">
            <label asp-for="TypeSelections" class="control-label"></label>
            <div class="col-md-10">
                <select id="optionTypeSelectionsEdit" class="form-control" asp-items="@Model?.TypeSelections">
                </select>
            </div>
            <input asp-for="TypeID" id="TypeEdit" class="form-control" hidden />
        </div>
        <div class="form-group mt-3">
            <label asp-for="Name" class="control-label"></label>
            <div class="col-md-10">
                <input asp-for="Name" class="form-control" />
                <span asp-validation-for="Name" class="text-danger"></span>
            </div>
        </div>
        <div class="form-group mt-3">
            <label asp-for="EmailTo" class="control-label"></label>
            <div class="col-md-10">
                <textarea asp-for="EmailTo" rows="8" class="form-control"></textarea>
                <span asp-validation-for="EmailTo" class="text-danger"></span>
            </div>
        </div>
        <div class="form-group mt-3">
            <label asp-for="EmailCC" class="control-label"></label>
            <div class="col-md-10">
                <textarea asp-for="EmailCC" rows="8" class="form-control"></textarea>
                <span asp-validation-for="EmailCC" class="text-danger"></span>
            </div>
        </div>

    </div>
    <div class="modal-footer">
        <button type="button" class="btn btn-secondary" data-bs-dismiss="modal">Cancel</button>
        <button type="submit" class="btn btn-warning">Edit</button>
    </div>
}

<script>
    $(document).ready(function () {

        $("#optionTypeSelectionsEdit").val($('#TypeEdit').val());

        // Update on change
        $(document).on("change", "#optionTypeSelectionsEdit", function (e) {
            $('#TypeEdit').val($("#optionTypeSelectionsEdit").val())
        });
    });
</script>

@section Scripts {
    @{
        await Html.RenderPartialAsync("_ValidationScriptsPartial");
    }
}
